{
  "name": "goldshore-web",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html dist/index.html && cp styles.css dist/styles.css && cp CNAME dist/CNAME"
  }
}
