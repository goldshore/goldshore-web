{
  "name": "goldshore-web",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html styles.css config.json 404.html 500.html robots.txt sitemap.xml _headers dist/ && cp CNAME dist/CNAME"
  }
}
